# rethinkdb-by-experiment

* bootstrap environment `docker-compose up -d`
* run application `docker-compose exec experimental-app npm start`
* open RethinkDB administration tools `http://172.21.0.2:8080/`
